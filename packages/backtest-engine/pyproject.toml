[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pnlclaw-backtest"
version = "0.1.0"
description = "Backtesting engine for PnLClaw — event-driven strategy backtesting with simulated execution"
readme = {text = "Backtesting engine for PnLClaw.", content-type = "text/plain"}
license = "AGPL-3.0-only"
requires-python = ">=3.11"
dependencies = [
    "pnlclaw-types",
    "pandas>=2.2.0",
    "numpy>=1.26.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pyarrow>=14.0",
]

[tool.hatch.build.targets.wheel]
packages = ["pnlclaw_backtest"]
